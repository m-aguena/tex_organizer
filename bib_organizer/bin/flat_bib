#!/usr/bin/env python

if __name__=='__main__':

    import os
    path = os.path.realpath(__file__)
    path = '/'.join(path.split('/')[:-2])+'/'

    import sys
    sys.path.append(path)
    
    from sort_bib import run

    run()
